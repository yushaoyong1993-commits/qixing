# 地图 · 路线 PRD（跋涉 · 骑行 App）

## 0. 文档信息

| 项 | 内容 |
|---|---|
| 产品名 | 跋涉（暂定） |
| 文档版本 | v1.1（审计对齐稿） |
| 日期 | 2026-09-07 |
| 上游文档 | 《骑行App需求文档_v0.3.md》§5.4（RT-01~08） |
| 关联文档 | 《骑行App_首页PRD_v1.1.md》（HP-06 缩略图复用）；《骑行App_记录PRD_v1.1.md》（会话交接）；《需求一致性审计报告_2026-09-07》 |
| 覆盖范围 | **地图 Tab 页** + 路线详情弹层 + 路线新建入口 + 「沿此路线骑行」交接 |
| 原型对应 | prototype v1.4 的「地图」页 |
| 修订记录 | v1.0：首稿（草稿）。v1.1：审计对齐——路线分享（RT-08）按母文档维持 **P0/M2**，本页 MP-14（导出 GPX）、MP-16（分享图/链接）统一为 P0｜M2；§1.3 Out-of-Scope 表述与 MP 行对齐 |

---

## 1. 背景与目标

### 1.1 背景

地图 · 路线 Tab 是"路"的载体：浏览地图、沉淀**我的路线**（手绘/规划/活动转存）、并从路线一键出发骑行。同时首页「上次骑行缩略图」（HP-06）复用本页的轨迹渲染能力，保证全 App 地图效果一致。

### 1.2 目标

| 目标 | 验收口径 |
|---|---|
| 路线库一眼可管理 | 我的路线列表展示缩略图/距离/爬升/类型/来源，可点开详情 |
| 一键出发 | 任意路线详情 →「沿此路线骑行」→ 进入记录会话（带路线预载），全程 ≤ 2 次点击 |
| 双底图 | 标准/卫星底图一键切换，缩放平移流畅（真机 60fps） |
| 与记录解耦 | 地图页不实现会话逻辑，只通过 §6 契约发起 |

### 1.3 Out of Scope（本 PRD 不做）

- 记录会话本身（含逐段导航的骑行中表现）→ 归《记录 PRD》；
- 路线云端同步（V2）、路线公开社交广场（V3）。

> **交付节奏说明**（避免与 Out of Scope 混淆）：
> - **M1 交付**：地图浏览/双底图/定位、路线列表与详情、手绘新建（MP-01~04/07~10）、离线包 POC（MP-15 前半）；
> - **M2 交付（P0）**：自动规划（MP-11）、编辑/删除（MP-12）、活动转存（MP-13）、导出 GPX 与分享图/链接（MP-14/16，对应母文档 RT-08）、离线完善（MP-15 后半）、无网降级（MP-05）；
> - 以上 M2 项的页面级需求均已收入本 PRD（MP-05/06/11~16）。

---

## 2. 信息架构与页面结构

底部 Tab「地图」承载：**大地图浏览 + 我的路线列表**；路线详情使用**全屏弹层**（在 Tab 内），不改变 Tab 状态。

```
┌─────────────────────────────────┐
│ [标准|卫星]               ⌖ 定位 │  ← 底图切换 + 定位（MP-01/02）
│ 🔍 搜索地点，或规划骑行路线…      │  ← 入口占位（MP-08/11，M2 启用规划）
│ ┌─────────────────────────────┐ │
│ │      大地图（缩放平移）        │ │  ← MP-03
│ │    🟠 轨迹线 / 起终点标记      │ │
│ └─────────────────────────────┘ │
│ 我的路线                    ＋新建 │  ← MP-07/09
│ ┌─────────────────────────────┐ │
│ │ 🗺 晨骑爬坡线  32.4km ↑860m │ │
│ │ 🗺 环湖休闲线  18.6km ↑96m  │ │  ← 列表行 → 路线详情
│ │ 🗺 通勤快捷线  12.1km ↑40m  │ │
│ └─────────────────────────────┘ │
├─────────────────────────────────┤
│  首页   地图   记录   我的          │ ← Tab Bar
└─────────────────────────────────┘
```

路线详情弹层（全屏 overlay）：

```
┌─────────────────────────────────┐
│ ✕                                │
│ 晨骑爬坡线（名称）      [编辑]      │
│ 手绘 · 09-06 骑行转存              │
│ ┌─────────────────────────────┐ │
│ │      路线大图（含起终点）       │ │
│ └─────────────────────────────┘ │
│ 距离 32.4km │ 爬升 860m │ 时长 约2:00 │
│ [ 🚴 沿此路线骑行 ]   ← 主操作      │
│ [ 导出 GPX ] [ 分享 ]            │  ← MP-14/16（M2）
│ [ 编辑路线 ]                     │
└─────────────────────────────────┘
```

---

## 3. 功能需求（MP 清单）

优先级沿用主文档：**P0**=V1.0 必须有（M1/M2 内部迭代见迭代列）；**P1/P2/P3** 同上。

### 3.1 地图与底图

| ID | 需求 | 说明 / 验收要点 | 优先级 | 迭代 |
|---|---|---|---|---|
| MP-01 | 底图样式切换 | 顶部「标准/卫星」分段切换（✅ 卫星默认关，用户自选），切换即时生效并记忆（UserSettings.mapStyle） | P0 | M1 |
| MP-02 | 定位到当前位置 | 右上角定位按钮：请求定位权限（即用即申请，§主文档 9）→ 地图以当前位置为中心；定位失败 Toast「定位失败，请检查权限」 | P0 | M1 |
| MP-03 | 大地图浏览 | 底图渲染（国内高德/腾讯、海外引擎抽象，§8）；双击/双指缩放、平移；地图初始视野 = 上次浏览位置或本地范围 | P0 | M1 |
| MP-04 | 轨迹线绘制 | 本页所有轨迹（路线/活动缩略）统一走自绘 polyline（白描边橙色主线，按速度/心率着色为 M2 预留）；首页 HP-06 调用同一渲染函数 | P0 | M1 |
| MP-05 | 无网络降级 | 无网且无离线包时：地图显示占位 + 提示「离线底图可下载（MP-15）」；不崩溃 | P0 | M2 |
| MP-06 | 活动轨迹浏览 | 在活动相关场景高亮其轨迹（与活动详情页联动，复用 MP-04） | P2 | M2 |

### 3.2 我的路线库

| ID | 需求 | 说明 / 验收要点 | 优先级 | 迭代 |
|---|---|---|---|---|
| MP-07 | 路线列表 | 顶部「我的路线」区块：按最近使用倒序，每行 = 小缩略图 + 名称 + `距离 · 爬升 · 类型`；空态见 §4 | P0 | M1 |
| MP-08 | 路线详情弹层 | 点击行弹出：大图（含起终点标记）、名称/来源/创建说明、数据（距离/爬升/预计时长/类型）、操作按钮 | P0 | M1 |
| MP-09 | 新建路线入口 | 「＋ 新建」：弹出方式选择——手绘路线（M1）/ 自动规划（M2 解锁）；M2 前自动规划项置灰并提示 | P0 | M1→M2 |
| MP-10 | 手绘路线（RT-05） | 地图进入绘制态：点按打点 → 折线预览 →「保存」命名（默认`我的路线 + 日期`）；双击/回退删点；长度<100m 禁止保存 | P0 | M1 |
| MP-11 | 自动规划（RT-04） | 输入起终点/选择 POI → 厂商骑行规划接口出线（优先自行车道、避开高速/禁行）；失败降级提示手动绘制 | P0 | M2 |
| MP-12 | 路线编辑/删除 | 详情页内：改名称、删点重画（M2）、删除（二次确认）；删除不删历史活动 | P1 | M2 |
| MP-13 | 活动转存为路线 | 活动详情页操作「保存为路线」→ 进入路线库（复用 Route 数据模型，来源=活动转存） | P1 | M2 |
| MP-14 | 导出路线 GPX | 详情/列表导出 .gpx（系统分享面板），与 MP-16 共用分享出口 | P0 | M2 |
| MP-15 | 离线地图（RT-07） | M1：按区域下载离线包 POC（验证记录/轻导航无网可用）；M2：区域列表/下载进度/存储占用/更新清理。⚠️ 依赖厂商离线方案验证（§主文档 13 #15） | P0 | M1→M2 |
| MP-16 | 路线分享（RT-08） | 生成分享图与链接（含 GPX 文件），走系统分享面板；V3 社交广场另行评估 | P0 | M2 |

---

## 4. 空态 / 边界 / 异常

| 场景 | 行为 |
|---|---|
| 无任何路线 | 列表区显示空态卡：「还没有路线」+ 主按钮「手绘一条」+ 次按钮「从历史活动转存」（M2） |
| 无定位权限 | 定位按钮点击 → 权限解释弹窗 +「去设置」；拒绝态可重试，不崩溃（同 HP-15 策略） |
| 地图加载失败 | 占位图 +「重试」；列表仍可用（路线数据本地） |
| 记录进行中 | 在地图页点「沿此路线骑行」→ Toast「请先结束当前骑行」并跳记录 Tab（防双会话，全 App 一致） |
| 无网络 | 已缓存底图正常显示；未缓存区域灰块；提示见 MP-05 |
| 海量路线（>500） | 列表虚拟滚动/分页；缩略图懒渲染 |
| 绘制中离开页面 | 未保存的绘制草稿保留在内存并提示，返回可继续（同 RCD-07 草稿思路，轻量版） |

---

## 5. 路线数据模型（增量于 §7 主文档）

| 字段 | 说明 |
|---|---|
| Route.id / name / type（手绘/自动规划/活动转存/导入） | 基础 |
| pts[] | 归一化+实际经纬度点列（同一份自绘渲染接口） |
| 距离/累计爬升/预计时长 | 保存时计算（含抽稀后精度说明） |
| createdAt / lastUsedAt | 排序依据 |
| offlineRegion | 关联离线包（M2） |

---

## 6. 跨页契约（与首页 / 记录 / 我的）

| 触发 | 去向 | 参数 | 规则 |
|---|---|---|---|
| 路线详情「沿此路线骑行」 | 记录 Tab | routeId + type | 进入记录页自动预载路线；用户点「开始」后进入**导航模式**（记录 PRD §5 RC-17）；M1 为方向/距离轻导航提示，M2 升级 turn-by-turn |
| 首页长按「开始骑行」的路线选项 | 记录 Tab | routeId | 同上（首页 HP-03，P1） |
| 首页 HP-06 缩略图 | 本页渲染函数 | 活动轨迹点 | 纯渲染复用，不切 Tab |
| 活动详情「保存为路线」 | 本页路线库 | activityId | MP-13（M2） |
| 我的 → 离线地图管理（M2） | 本页 | — | 由「我的」设置项跳转 |

---

## 7. 文案规范（初稿，✅ 可改）

| 位置 | 文案 |
|---|---|
| 底图切换 | 标准 / 卫星 |
| 搜索占位 | 搜索地点，或规划骑行路线… |
| 空态标题 | 还没有路线 |
| 空态主按钮 | 手绘一条 |
| 新建菜单 | 手绘路线 / 自动规划 |
| 保存默认名 | 我的路线 + 日期 |
| 无网提示 | 当前区域暂无离线地图（可下载） |
| 规划不可用（M1） | 自动规划将在后续版本开放，可先手绘 |

---

## 8. 埋点（自用本地日志，轻量）

| 事件 | 触发 |
|---|---|
| map_pv / map_style_switch | 进入地图页 / 切换底图样式 |
| route_open | 打开某路线详情 |
| route_start_ride | 沿此路线骑行 |
| route_draw_start / route_draw_save | 手绘开始 / 保存 |
| route_export / route_share | 导出 GPX / 分享（M2） |
| route_offline_download | 下载离线包（M2） |

---

## 9. 视觉与布局（沿用浅色主题，v1.1 token）

- 底图：标准=米白瓦片（已定），卫星=深色瓦片（已定，真实接入后跟随厂商卫星层）；
- 轨迹线：白描边 + 橙 `#fc4c02` 主线；起终点标记圆点（起点白底橙圈 / 终点橙底白圈）；
- 路线行：左缩略图 74×54、圆角 9px；行高 ≥ 48pt；
- 详情弹层：复用 overlay（白底 92% 模糊）；主按钮橙渐变；
- 触控最小区域 ≥ 48pt；地图控件 38×38px 与底图保持层级分离（z-index）。

---

## 10. 依赖与排期

| 依赖 | 来源 | 归属 |
|---|---|---|
| 地图 SDK + 引擎抽象（标准/卫星） | §8 主文档 | M0~M1 |
| 轨迹自绘渲染函数（跨页复用） | 本 PRD MP-04 | M1 |
| 骑行规划接口 POC（厂商能力验证） | §主文档 13 #15 | M1（决定 M2 方案） |
| 离线包方案 POC | RT-07 / MP-15 | M1 |
| 记录会话交接（预载路线） | 《记录 PRD》 | M1 |

里程碑：M1 交付 MP-01~04/07~10/15(POC)；M2 交付 MP-05/06/11~14/15(完善)/16。

---

## 11. 待确认清单（TBD）

| # | 问题 | 默认值 |
|---|---|---|
| TBD-1 | 大地图初始视野策略（上次浏览位置 vs 常驻本地） | 记忆上次视野 + 首次=本地范围 |
| TBD-2 | 手绘打点交互：拖拽式 vs 点按式 | 点按打点 + 可拖动微调 |
| TBD-3 | 路线是否区分"骑行方向"（环线双向） | 默认双向可用，方向仅影响导航播报（M2） |
| TBD-4 | 离线包粒度：高德行政区域 vs 自绘格网 | M1 POC 后定（#15） |
| TBD-5 | 卫星底图流量策略（仅 Wi-Fi 下载） | 默认 Wi-Fi 下缓存 |
| TBD-6 | 是否支持导入 .gpx 路线文件入库 | 支持（复用 IO 解析，M2） |

---

## 12. 验收清单（QA 用例大纲）

- [ ] 首次进入地图页：底图渲染、列表为空态、定位按钮可点（A-MP-01）
- [ ] 标准/卫星切换即时生效并记忆（重启后保持）（A-MP-02）
- [ ] 点定位按钮：未授权→解释弹窗；授权→地图居中（A-MP-03）
- [ ] 手绘：打点→预览→保存成功进入列表；<100m 禁止保存并提示（A-MP-04）
- [ ] 打开路线详情：图与列表缩略一致（同渲染函数），数据正确（A-MP-05）
- [ ] 沿此路线骑行：无会话→进记录页预载路线；有会话→拦截提示（A-MP-06）
- [ ] 删除路线二次确认；删除不影响历史活动（A-MP-07）
- [ ] 无网时页面可用性（已缓存正常 / 未缓存灰块提示）（A-MP-08）
- [ ] 记录中进入地图页不崩溃、不产生第二个会话（A-MP-09）
- [ ] 导出 GPX / 分享图·链接（M2）：文件可被其他应用打开（A-MP-10）

---

*本 PRD 为 v1.1 可修改稿；MP-xx 可追溯主文档 RT-xx。改完可再做审计。*
