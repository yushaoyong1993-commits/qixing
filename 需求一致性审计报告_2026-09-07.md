# 需求一致性审计报告（跨文档交叉审计）

| 项 | 内容 |
|---|---|
| 日期 | 2026-09-07 |
| 审计范围 | 《骑行App需求文档_v0.2.md》＋《首页PRD_v1.1》＋《地图路线PRD_v1.0》＋《记录PRD_v1.0》＋《我的PRD_v1.0》 |
| 方法 | ① 全量"引用↔定义"断链扫描（自动）② 优先级/默认值/里程碑口径人工比对（含原文取证）③ 页面规格归属核查 |
| 结论 | **无引用断链**（全局扫描通过）；发现 **2 组默认值矛盾、1 处优先级级别矛盾（自动计圈）、1 处分享功能级别冲突、1 项页面规格归属缺失** 及若干低优先级口径问题。修复前不建议进入开发口径冻结 |

---

## 审计结论总览

| 维度 | 状态 |
|---|---|
| ID 引用完整性 | ✅ 通过（全局 token 均有定义处） |
| 默认值一致性 | 🔴 2 处矛盾（自动暂停、自动计圈） |
| 优先级一致性 | 🟡 自动计圈 P1↔P0；分享 RT-08 P0↔(P1/P3) |
| 里程碑口径 | 🟡 基本一致，个别措辞待统一 |
| 页面规格归属 | 🟡 统计二级页无独立 PRD（三方引用无主） |
| 可拆解结论 | 否——先关闭下表 4 个高/中优先级决策后即可进入开发拆解 |

---

## 一、高优先级（阻塞口径冻结）

| ID | 问题 | 证据 A | 证据 B | 建议 |
|---|---|---|---|---|
| Q-H-01 | **自动暂停默认值矛盾** | 母文档 RCD-04：「✅ 自动暂停…**默认关**，可开关」P0 | 记录 PRD RC-02：「**默认开**（✅）」；原型开关初始=开 | 二选一后统一三处。推荐 **默认开**（路口/红灯场景友好，原型已按开评审）→ 改母文档 RCD-04 |
| Q-H-02 | **自动计圈默认值矛盾** | 母文档 RCD-09：「✅ **默认每 5 km 自动计圈**」P1 | 记录 PRD RC-03：「**默认关**（✅）」；原型开关初始=关 | 二选一后统一。推荐 **默认关**（显式开启更克制）→ 改母文档 RCD-09 |
| Q-H-03 | **自动计圈优先级矛盾** | 母文档 RCD-09 = **P1** | 记录 PRD RC-03 = **P0**（记录页开关/圈数展示已在 M1 UI） | 与 Q-H-02 联动：记录页已含此能力 → 母文档 RCD-09 升 **P0** |
| Q-H-04 | **路线分享级别冲突** | 母文档 RT-08 = **P0｜M2**（导出链接/图片/GPX）；§12 M2 含"RT-08 分享" | 地图 PRD：MP-14 导出 GPX=P1｜M2；MP-16 分享链接/图片=P3｜— | 尊重早前"RT 全 P0"决定：地图 PRD MP-14/16 回写为 **P0｜M2**（导出 GPX + 分享图/链接），P3 仅留社交化互动；或反向降母文档——需你拍板 |

---

## 二、中优先级（影响方案/规格完整）

| ID | 问题 | 证据与说明 | 建议 |
|---|---|---|---|
| Q-M-01 | **统计二级页无规格归属** | 首页 HP-04/05（点击进入）、我的 ME-03、母文档 STA-01~10 均指向"统计二级页"，但该页内容（周期切换/近 7 天柱图/累计/个人纪录）只存在于原型，无 PRD 章节与验收 | 独立《统计 PRD》（推荐）或并入首页 PRD 二级页章节 |
| Q-M-02 | **声音/震动反馈重复且级别冲突** | 母文档 RCD-10「震动/声音反馈（开始/暂停/计圈）」P1 vs 记录 PRD RC-21「语音/震动反馈（默认震动开、语音关）」P2 | 拆分口径：震动+按钮提示音 = P1（随 RCD-10）；独立"语音播报（轻）"= P2；RC-21 只保留语音部分 |
| Q-M-03 | **记录 PRD §6 交接表缺一行** | 首页 HP-03「长按开始→沿上次/我的路线」（P1）与地图 PRD §6 均有此交接，但记录 PRD §6 未收录该触发行 | 记录 PRD §6 补"首页长按 → routeId 预载"一行 |
| Q-M-04 | **摘要保存步骤缺命名/备注** | 母文档 RCD-05：「保存前…→ 命名/类型/备注 → 保存」；记录 PRD RC-12 仅默认名直存（依赖事后 ACT-05 编辑 P0） | RC-12 补充「可展开改名/备注（默认名可直接保存）」或注明"复用 ACT-05"为唯一改入口（二选一，建议前者体验顺） |
| Q-M-05 | **GPS 预热时点措辞不一致** | 母文档 RCD-01「点击后进入记录页**并开始定位**」；记录 PRD RC-04 定位校验在点「开始」后 | 统一：进入准备页即开始 GPS 预热并展示信号；点「开始」要求信号就绪（超时提示） |

---

## 三、低优先级（体验/文档细节）

| ID | 问题 | 说明 |
|---|---|---|
| Q-L-01 | 原型模拟心率 vs 规则 | RC-05 无外设时心率显示 `—`，原型为演示展示了模拟值——非冲突，实现以 RC-05 为准 |
| Q-L-02 | 概览卡不含"次数" | 首页 HP-04 为 3 指标（距离/时长/爬升），统计页含次数——概览为统计子集，可接受；如需对齐可加"次数" |
| Q-L-03 | 页面 PRD 间版本引用未固定 | 各 PRD §0"关联文档"未写对方版本号，文档迭代后易错位——建议统一加版本列 |
| Q-L-04 | 轻导航形态两个文档措辞 | 记录 PRD RC-17（顶部条 M1）与 TBD-5（顶部条+震动 M1/语音 M2）自洽；地图 PRD §1.3 一致——无需改动，仅记录 |
| Q-L-05 | 我的-总时长单位展示 | 我的 TBD-1（h/min 格式）与统计 fmtDurShort 逻辑一致——无需改 |

---

## 四、原文矛盾速查表

| 位置 A | 位置 B | 差异 |
|---|---|---|
| 母文档 RCD-04：自动暂停默认关 | 记录 RC-02 / 原型：默认开 | 默认值 |
| 母文档 RCD-09：默认每 5 km 计圈（P1） | 记录 RC-03 / 原型：默认关（P0） | 默认值 + 优先级 |
| 母文档 RT-08：P0｜M2 全量分享 | 地图 MP-14/16：导出 P1、分享 P3 | 功能级别 |
| 母文档 RCD-10：震动/声音 P1 | 记录 RC-21：语音/震动 P2 | 拆分口径 |

---

## 五、与标品差异

N/A（无内部标品需求说明书；Strava 仅为 §1.4 参照系）。

---

## 六、隐含假设（待验证，沿用母文档 §13 #15 优先）

1. 地图厂商骑行规划/偏航重规划接口可用性 → M1 POC（决定 M2 完整导航）；
2. 离线底图方案（高德区域包 / OSM / Mapbox）满足轻导航 → M1 POC；
3. 统计页数据源与首页概览共用聚合函数（口径一致）→ 实现期架构约束。

---

## 七、与既有文档对照

| 来源 | 状态 |
|---|---|
| 首页 PRD v1.1 §12 TBD | 无新增冲突（TBD-2 已定稿与本报告无矛盾） |
| 记录 PRD §12 TBD | TBD-1 自动暂停判定参数为"实现期校准"，与 Q-H-01 决策正交 |
| 我的 PRD §11 TBD | TBD-4 导入默认计入统计，与母文档 STA-10 一致 ✅ |

---

## 八、建议澄清顺序

1. Q-H-01/02/03：两默认值 + 自动计圈级别（一次性改母文档 + 记录 PRD + 原型初始态）；
2. Q-H-04：分享范围与级别（影响地图 PRD MP-14/16 与母文档 RT-08）；
3. Q-M-01：统计页规格归属；
4. Q-M-02~05：随文档修订一并处理（低工作量）。

---

*本报告依据 2026-09-07 各文档当时版本出具；修订后需复核涉及的 Q 编号行。*
