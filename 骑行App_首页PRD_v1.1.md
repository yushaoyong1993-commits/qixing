# 首页 PRD（跋涉 · 骑行 App）

## 0. 文档信息

| 项 | 内容 |
|---|---|
| 产品名 | 跋涉（暂定） |
| 文档版本 | v1.1（浅色原型确认稿） |
| 日期 | 2026-09-07 |
| 上游文档 | 《骑行App需求文档_v0.2.md》 |
| 覆盖页面 | **首页 Tab（App 启动默认落地页）**；不含「地图/路线」「记录」「我的」三个 Tab（各自另行出 PRD） |
| 关联需求 | RCD-01/05/07、ACT-01/02/05、STA-01/03/04/05/09/10、RT-01/06、IO-01、ACC-01/02（见 §3 引用） |
| 修订记录 | v1.0：首次成稿（草稿）。v1.1：按浅色交互原型定稿——纯文字四 Tab、浅色主题规范、概览卡改为「今日/本周/本月」分段切换；视觉与交互详见 §10 |

---

## 1. 背景与目标

### 1.1 背景

首页是用户每天打开 App 见到的第一个页面，需要同时回答三个问题：

1. **我现在要骑**（开始记录——最高频动作，对标 Strava 首页的录制隐喻，RCD-01）；
2. **我今天/这周骑得怎么样**（概览，STA-01 的入口）；
3. **我最近骑了什么**（最近活动，ACT-01 的入口）。

### 1.2 目标

| 目标 | 验收口径 |
|---|---|
| 一次点击进入记录 | 冷启动 → 首页首屏即可点「开始骑行」，无需滚动/二次操作 |
| 概览数据一眼可读 | 今日/本周/本月核心指标首屏可见，口径与统计页一致（STA-09） |
| 最近活动可一步直达 | 点击任一最近活动行直接进入该活动详情（ACT-02） |
| 本地优先、无网可用 | 首页所有内容基于本地数据渲染，无网络不影响（§需求文档 v0.2 §4） |

### 1.3 Out of Scope（本 PRD 不做）

- 「地图/路线」「记录」「我的」三个 Tab 的页面本身；
- 桌面小组件/锁屏/灵动岛等**系统级快捷入口**（按 §13 #4 决策，后置于 V1.x）；
- 统计页、活动列表页、活动详情页的**页面规格**见《统计 PRD v1.1》《活动 PRD v1.1》（本 PRD 仅定义从首页的入口与跳转参数）；
- 深色模式（本期不做，UI 规范预留主题 token）。

---

## 2. 信息架构（决策记录）

### 2.1 底部导航（用户已拍板）

底部 4 个 Tab（自左向右）：**首页 ｜ 地图 ｜ 记录 ｜ 我的**

```
┌─────┬─────┬─────┬─────┐
│ 首页 │ 地图 │ 记录 │ 我的 │   ← 底部 Tab Bar（纯文字，无图标）
└─────┴─────┴─────┴─────┘
```

- ✅ **Tab 样式（已定，v1.1 原型确认）**：四项**纯文字、无图标、完全一致**；未选中灰 `#9898a3`，选中深橙 `#d64400` 加粗并带 20px 橙色短横线指示；字号 14、字距 3px、等宽分布。
- ✅ 推导默认（可改）：由于没有「活动」「统计」独立 Tab，二者由**首页二级页**承接：
  - 概览卡点击 → **统计页**（二级，含 STA-03 热力图 / STA-04 PR / STA-05 趋势）；
  - 「最近活动 → 查看全部」→ **活动列表页**（二级，复用 ACT-01）；
  - 若后续觉得活动/统计使用频率高，可升级为独立 Tab（记录在 §12 TBD-1）。

### 2.2 首页与其他 Tab 的职责分工

| Tab | 职责 | 与首页的关系 |
|---|---|---|
| 首页 | 开始入口 + 今日概览 + 最近活动（本 PRD） | — |
| 地图 | 地图浏览、我的路线、手绘/规划路线、沿路线骑行（RT-01/05/04/06） | 首页不展示地图操作，仅保留「上次轨迹缩略图」（复用其渲染能力） |
| 记录 | 记录会话宿主：开始记录、实时数据、结束保存（RCD-01~14） | 首页「开始骑行」点击后**切换到此 Tab** 进入记录会话（见 §7 跨 Tab 交接） |
| 我的 | 资料（ACC-02）、设置（§5.8）、数据导入/导出（IO-01/05）、数据管理 | 首页不做设置入口，保持纯净 |

---

## 3. 用户与核心场景

| 场景 | 用户 | 预期路径 |
|---|---|---|
| S1 日常出门骑行 | 本人 | 亮屏 → 首页 → 点「开始骑行」→（记录 Tab）开始记录 |
| S2 查看今日/本周进度 | 本人 | 首页 → 概览卡 → 统计页 |
| S3 复盘上一次骑 | 本人 | 首页 → 上次轨迹缩略图/最近活动 → 活动详情 |
| S4 首启无数据 | 本人 | 首页空态引导 → 开始骑行 或 导入历史 .fit/.gpx |
| S5 记录中途切回来 | 本人 | 记录中回首页 → 顶部「骑行进行中」横幅 → 回到记录 |

---

## 4. 页面结构（自上而下线框）

```
┌─────────────────────────────────┐
│ 状态栏                              │
│ ┌─────────────────────────────┐ │
│ │ 骑行进行中  42:35  23.5 km  │ │ ← 条件显示（S5，HP-12）
│ │        [ 回到记录 ]         │ │
│ └─────────────────────────────┘ │
│ ┌─────────────────────────────┐ │
│ │ 有未保存的记录草稿（09-06）    │ │ ← 条件显示（HP-13，RCD-07）
│ │  [继续] [保存] [丢弃]        │ │
│ └─────────────────────────────┘ │
│                                 │
│   🚴 开始骑行                     │ ← Hero CTA（HP-01，品牌橙渐变大按钮）
│   GPS 轨迹 · 支持心率/踏频外设      │    副文案（HP-02）
│ ┌─────────────────────────────┐ │
│ │  [今日] [本周] [本月]         │ │ ← 概览卡（HP-04，分段切换）
│ │  距离 3.2km 时长 42min 爬升… │ │    点击 → 统计页
│ │  较上周 +12% · 累计 1,256 km │ │
│ └─────────────────────────────┘ │
│ ┌─────────────────────────────┐ │
│ │ 🗺 上次骑行缩略图             │ │ ← HP-06，点击 → 该活动详情
│ │    09-06 · 42.3 km · 640 m  │ │
│ └─────────────────────────────┘ │
│  最近活动                    [全部]│ ← HP-08/10
│  • 晚间骑行      09-06 42.3 km  │
│  • 通勤          09-05 12.1 km  │
│  • 晨骑          09-04 28.7 km  │
├─────────────────────────────────┤
│  首页   地图   记录   我的          │ ← Tab Bar（纯文字）
└─────────────────────────────────┘
```

> 布局规则：
> - 内容纵向可滚动；**Hero CTA 固定首屏顶部**，不随滚动消失（保证"一次点击进入记录"）。
> - 横幅（进行中/草稿）只在对应状态存在时出现，且**置于 Hero 之上**；两者同时存在时按「进行中 > 草稿」顺序显示（正常情况不会并存，见 HP-13 异常说明）。
> - 首次使用（无任何活动数据）：地图缩略图块隐藏，「最近活动」区显示空态引导卡（HP-11）。

---

## 5. 功能需求（HP 清单）

优先级沿用主文档口径：**P0**=V1.0 必须有；**P1**=V1.0~V1.x 尽快补。

### 5.1 Hero 区：开始骑行

| ID | 需求 | 交互 / 说明 | 状态与异常 | 验收要点 | 优先级 |
|---|---|---|---|---|---|
| HP-01 | 开始骑行大按钮 | 首页首屏顶部主按钮（品牌橙渐变、白字、圆角 16px、内带骑行图形图标）；点击 → 进入记录会话（跨 Tab 交接见 §7） | 记录进行中点击 → Toast「请先结束当前骑行」并跳记录 Tab；定位权限未授予 → 弹出授权引导（见 HP-15） | 冷启动后 1 次点击到达记录界面（不含系统授权弹窗） | P0 |
| HP-02 | 按钮副文案 | 按钮下方小字：`GPS 轨迹 · 支持心率/踏频外设`（✅ 文案待定）；有外设已连接时追加 `已连接：心率带`（DEV-02 状态回传） | 副文案随外设连接状态变化 | 文案与真实能力一致；无外设时不出现外设字样 | P1 |
| HP-03 | 长按快捷菜单 | 长按按钮弹出：`普通骑行 / 沿上次路线骑行 / 沿我的路线…`（选项来自路线库） | 无路线/无上次路线时对应项置灰并给原因 | 长按选项与实际可达性一致 | P1 |

### 5.2 概览卡

| ID | 需求 | 交互 / 说明 | 状态与异常 | 验收要点 | 优先级 |
|---|---|---|---|---|---|
| HP-04 | 概览卡（分段切换） | 卡片内「今日/本周/本月」三段切换（已定，v1.1），选中态白底高亮；每段展示 **距离 / 时长 / 爬升** 三项 + 卡底一行「较上一周期 ±%」与「累计骑行」；数值按用户单位制换算（§4-4） | 当日无骑行 → 今日数值显示 `—` 并提示「今天还没骑」；无任何历史数据 → 显示占位 `——` 与「完成第一次骑行后生成」（HP-11）；分段内无数据时对应指标为 `—` | 数值与统计页同口径（STA-09）；导入活动计入（STA-10）；切换单位后立即换算；切换分段即时刷新 | P0 |
| HP-05 | 概览卡入口 | 点击概览卡 → 跳**统计页**（STA-01~05 全量） | — | 一次点击可达统计页 | P0 |

### 5.3 上次轨迹缩略图

| ID | 需求 | 交互 / 说明 | 状态与异常 | 验收要点 | 优先级 |
|---|---|---|---|---|---|
| HP-06 | 上次骑行缩略图卡 | 展示最近一次已完成活动的轨迹缩略（浅色瓦片小地图 + 自绘轨迹线，复用 RT-01 渲染能力）与摘要：名称、日期、距离、时长、累计爬升 | 无活动 → 整块隐藏；地图渲染失败 → 显示纯色占位图（保留文字摘要，不阻塞） | 缩略图 200ms 内渲染；与地图页/活动详情一致（同引擎/同轨迹源） | P0 |
| HP-07 | 缩略图点击 | 点击 → 跳该活动详情页（ACT-02） | — | 一次点击到达对应活动 | P0 |

### 5.4 最近活动

| ID | 需求 | 交互 / 说明 | 状态与异常 | 验收要点 | 优先级 |
|---|---|---|---|---|---|
| HP-08 | 最近活动列表 | 按时间倒序展示最近 **3 条**已完成活动（✅ 条数可配）：每行显示 活动名（默认名见附录 A-3）、日期、距离、时长、爬升；来源图标区分（手机记录/导入，ACT-07） | 列表为空 → 空态引导卡（HP-11）；进行中的活动不展示（未保存） | 排序与活动列表页（ACT-01）一致；字段无缺省 | P0 |
| HP-09 | 行点击 | 点击某行 → 跳该活动详情页（ACT-02） | 该活动已被删除（数据竞态）→ 刷新列表并 Toast「该活动不存在」 | 一次点击到达对应活动 | P0 |
| HP-10 | 查看全部 | 区块标题右侧「全部」入口 → 活动列表页（ACT-01 全量列表，含年/月/类型筛选） | 无 | 一次点击到达列表页 | P0 |
| HP-11 | 空态引导卡 | 无任何活动时展示引导：主按钮「开始你的第一次骑行」+ 次按钮「导入历史数据（.fit/.gpx/.tcx）」（IO-01） | 点击主按钮 → 同 HP-01；点击导入 → 系统文件选择器 | 两种引导均可达 | P0 |

### 5.5 状态横幅与刷新

| ID | 需求 | 交互 / 说明 | 状态与异常 | 验收要点 | 优先级 |
|---|---|---|---|---|---|
| HP-12 | 记录进行中横幅 | 记录会话进行中且用户切回首页时显示：`骑行进行中 42:35 · 23.5 km  [回到记录]`；数据随记录实时刷新 | 记录结束/保存后横幅消失并刷新概览与最近活动 | 横幅数据与记录页一致；点击回到记录会话 | P0 |
| HP-13 | 未保存草稿提示条 | 存在中断草稿（RCD-07）时显示：`有未保存的记录草稿（09-06 08:12 起） [继续] [保存] [丢弃]` | 丢弃需二次确认；继续 → 切记录 Tab 恢复会话 | 与 RCD-07 验收一致：强杀后重开可见并可操作 | P0 |
| HP-14 | 页面数据刷新 | 每次 App 回前台 / Tab 切回首页时重算概览与列表（本地增量计算，万条活动流畅，§6-性能） | 计算失败（库损坏等）→ 展示兜底错误视图 + 重试 | 保存活动后切回首页即见最新数据 | P0 |

### 5.6 权限与授权（首页触点）

| ID | 需求 | 交互 / 说明 | 状态与异常 | 验收要点 | 优先级 |
|---|---|---|---|---|---|
| HP-15 | 定位授权触发 | 首次点击「开始骑行」时按需申请定位权限（iOS：使用期间；后台定位用途按商店审核要求预先声明，§需求文档 v0.2 §9） | 拒绝 → 再次点击时弹解释文案 +「去设置」跳系统设置；授权后自动回到原流程 | 权限文案解释「为什么需要定位」；拒绝态可重试引导，不崩溃 | P0 |
| HP-16 | 蓝牙权限说明 | 仅当用户进入「外设连接」场景时申请蓝牙（首页不主动申请） | — | 首页不触发蓝牙权限弹窗 | P1 |

---

## 6. 空态 / 边界 / 异常汇总

| 场景 | 行为 |
|---|---|
| 全新安装、无数据 | 概览卡显示占位符；上次轨迹块隐藏；最近活动显示引导卡（HP-11） |
| 今日无骑行、有历史 | 今日指标 `—` +「今天还没骑」；其余正常 |
| 只有导入的活动、无手机记录 | 视为正常历史数据（来源图标区分），概览/列表正常 |
| 记录进行中 + 存在草稿 | 正常不应并存（草稿仅在异常退出产生）；若并存按「进行中 > 草稿」顺序展示横幅 |
| 地图缩略渲染失败 | 降级为占位卡片，保留文字摘要（距离/爬升），不阻塞页面 |
| 数据损坏/读取异常 | 整页兜底错误视图 +「重试」按钮；不白屏 |
| 单位制/语言切换 | 全页即时按新单位/语言重渲染（预留 i18n/unit token，§4-4） |
| 超长屏 / 系统字号放大 | 布局纵向可滚动；Hero 按钮不压缩至不可点（最小可点区域 ≥ 48pt） |

---

## 7. 跨 Tab 交接（与「记录」Tab 的契约）

首页只负责"发起"，会话细节归记录 Tab（另出 PRD），此处定义接口契约：

| 触发 | 交接目标 | 参数 | 预期 |
|---|---|---|---|
| 首页「开始骑行」/空态主按钮 | 记录 Tab | 无（全新记录） | 底部 Tab 切到「记录」，自动进入待开始状态，用户点开始后 GPS 定位 |
| 长按「沿上次路线骑行」 | 记录 Tab | routeId=上次活动转存路线 | 预载该路线，弹出确认「沿此路线骑行？开始」→ 开始后进入导航模式（RT-06） |
| 长按「沿我的路线…」 | 记录 Tab | routeId=所选 | 同上 |
| 进行中横幅「回到记录」 | 记录 Tab | sessionId=当前会话 | 直接回到会话实时页，不新建 |
| 草稿「继续」 | 记录 Tab | draftId | 恢复会话继续记录（RCD-07） |

> 边界规则：一次仅允许一个进行中会话；任何"开始新记录"入口在会话进行中都必须先引导结束或放弃当前会话。

---

## 8. 文案规范（初稿，✅ 可改）

| 位置 | 文案 |
|---|---|
| Hero 按钮 | 开始骑行 |
| 副文案 | GPS 轨迹 · 支持心率/踏频外设 |
| 今日空 | 今天还没骑，出发吧 |
| 无数据占位 | 完成第一次骑行后生成统计 |
| 空态主按钮 | 开始你的第一次骑行 |
| 空态次按钮 | 导入历史数据 |
| 进行中横幅 | 骑行进行中 · {时长} · {距离} |
| 草稿条 | 有未保存的记录草稿（{起止时间}） |
| 记录中误触开始 | 请先结束当前骑行 |
| 定位拒绝再触发 | 需要定位权限才能记录骑行轨迹，请在系统设置中开启 |

---

## 9. 埋点（自用阶段本地日志，§11 原则）

| 事件 | 触发 | 用途 |
|---|---|---|
| home_pv | 首页每次可见 | 活跃自检 |
| start_click | 点击开始骑行 | 记录入口转化 |
| start_abort_10s | 进入记录后 10s 内退出 | 检查开始流程问题 |
| overview_click | 点击概览卡 | 统计入口使用度 |
| overview_period_switch | 切换今日/本周/本月分段 | 概览卡使用方式 |
| thumb_click | 点击上次轨迹缩略 | 复盘使用度 |
| recent_click / viewall_click | 点击最近活动行 / 全部 | 列表入口使用度 |
| draft_action | 草稿条操作（继续/保存/丢弃） | RCD-07 可靠性 |

> 不做无决策价值的埋点；以上仅本地可查，不上报（与 §9 隐私原则一致）。

---

## 10. 视觉与布局规范（v1.1 已按浅色原型定稿）

1. **浅色主题（已定）**：页面白底 `#ffffff`，浅灰卡片分隔 `#f2f2f5`，细分割线 `rgba(17,17,21,.09)`；正文 `#1b1b1f`、次级 `#5d5d67`、弱化 `#9898a3`；卡片白底 + 1px 浅描边 + 轻阴影。
2. **品牌色**：橙 `#fc4c02`（主按钮 / 进行中态 / 激活元素）；白底上文字强调色用深橙 `#d64400`（保证 WCAG AA 对比度）；主按钮为橙渐变 + 白字。
3. **Hero**：品牌橙渐变大按钮、白字、圆角 16px、高 ≥ 56pt；内含白色骑行图形圆钮 + 主文案 + 副文案（见 §5.1）。
4. **Tab Bar（纯文字，已定）**：四项纯文字无图标、完全一致；字号 14、字距 3px、等宽分布；未选中 `#9898a3`，选中 `#d64400` 加粗 + 底部 20px 橙色短横线；背景白 96% 半透明 + 顶部细分割线。
5. **卡片体系**：统一圆角 16px，层级 Hero → 概览卡 → 上次轨迹卡 → 列表行；卡片内分段控件用浅灰底 `#f2f2f5`、选中白底高亮。
6. **数字展示规范**（附录 A-6）：距离两位小数、时长 `42:35`、速度一位小数、数字用等宽数字字体。
7. **触控**：最小可点区域 ≥ 48pt；文字对比度 WCAG AA；预留主题 token 便于品牌改名与（如必要）深色模式。

---

## 11. 依赖与排期

| 依赖 | 来源 | 归属 |
|---|---|---|
| 本地数据模型 + 活动聚合查询（概览需汇总统计，缓存机制） | §7 Activity/UserSettings | M0~M1 |
| 地图 SDK 与轨迹自绘（缩略图，浅色瓦片） | RT-01 / §8 | M1 |
| 记录会话状态机（进行中横幅数据源） | RCD-01~07 | M1 |
| 草稿机制 | RCD-07 | M1 |
| 定位权限申请流程与用途声明 | §9 | M0 |
| 导入入口（空态引导） | IO-01 | M2（GPX 先行） |

> 首页主体随 **M1（记录闭环 + 轻导航）** 交付；缩略图依赖地图能力，与 M1 内 RT-01 同步完成（已在原型以浅色瓦片验证渲染链路）。

---

## 12. 待确认清单（首页 TBD）

| # | 问题 | 默认值 | 影响 |
|---|---|---|---|
| TBD-1 | 活动/统计无独立 Tab，均从首页进二级页——是否需要独立 Tab？ | 保持二级页，观察自用频率后再议 | Tab 结构与首页内容边界 |
| TBD-2 | 概览卡指标组合 | **已定（v1.1）**：今日/本周/本月 分段切换 × 距离/时长/爬升 | 首屏信息密度 |
| TBD-3 | 最近活动条数 3 | 3 条 | 首屏高度 |
| TBD-4 | 无账号时首页是否需要"本地昵称"问候（如 `下午好`）？ | 不做问候，保持信息型首页 | 头部设计 |
| TBD-5 | 长按快捷菜单（HP-03）是否纳入 V1.0 | P1，M2 前评估 | 交互复杂度 |
| TBD-6 | 品牌色与「跋涉」图标 | 浅色主题 token 已在 v1.1 原型定稿（橙 `#fc4c02` / 深橙 `#d64400`）；App 图标待设计 | 全 App 视觉 |
| TBD-7 | 首页是否有必要做「今日/本周/本月」之外的自定义日期范围 | 本期不做 | 统计口径范围 |

---

## 13. 验收清单（QA 用例大纲）

- [ ] 冷启动进入首页；无数据时概览占位、无缩略图、空态引导卡可见（A-HP-01）
- [ ] 点击「开始骑行」→ 切记录 Tab；拒绝定位后再次点击出现解释与「去设置」（A-HP-02）
- [ ] 完成一次骑行保存后切回首页：横幅消失、概览数值更新、最近活动新增该条（A-HP-03）
- [ ] 记录进行中切回首页：显示进行中横幅且数值随记录刷新；点击回到记录页；记录中点开始骑行被拦截（A-HP-04）
- [ ] 记录中强杀 App → 重开首页出现草稿条，三个操作均可用（A-HP-05）
- [ ] 概览卡今日/本周/本月切换即时刷新，数值与统计页同口径（A-HP-06）
- [ ] 点击概览卡/缩略图/活动行/全部，均一次跳转到对应页面且对象正确（A-HP-07）
- [ ] 导入一条 .fit 后：首页列表出现带"导入"来源图标的活动，概览计入（A-HP-08）
- [ ] 单位制切换 km/mi 后首页数值即时换算（A-HP-09）
- [ ] 概览数值与统计页同口径一致（同一活动、同一天两次查看一致）（A-HP-10）

---

*本 PRD 为 v1.1 可修改稿；编号 HP-xx 与主文档 RCD/ACT/STA/RT/IO 编号可互相追溯。改完可再做一轮审计。*
