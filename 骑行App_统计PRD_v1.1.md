# 统计 PRD（跋涉 · 骑行 App）

## 0. 文档信息

| 项 | 内容 |
|---|---|
| 产品名 | 跋涉（暂定） |
| 文档版本 | v1.1（P0 口径对齐稿） |
| 日期 | 2026-09-07 |
| 上游文档 | 《骑行App需求文档_v0.3.md》§5.3（STA-01~10） |
| 关联文档 | 《骑行App_首页PRD_v1.1.md》（入口 HP-04/05）；《骑行App_我的PRD_v1.0.md》（入口 ME-03）；《需求一致性审计报告2_2026-09-07》（Q-H2-01 落定：STA-03/05 及 STA-04 距离窗口 PR 全部 P0 进 M1） |
| 覆盖范围 | **统计二级页**（首页 Tab 内全屏子页）：周期切换 / 指标格 / 近 7 天柱状 / **日历热力图** / **周·月趋势与累积曲线** / 累计 / 个人纪录（含最快 10/50/100 km） |
| 原型对应 | prototype v1.5 的「统计」子页（热力图/周月趋势将在 v1.6 补齐区块） |
| 修订记录 | v1.0：首稿。v1.1：按审计结论对齐母文档——日历热力图（STA-03）、周/月趋势与累积曲线（STA-05）、个人纪录距离窗口（STA-04 最快 10/50/100 km）全部列为 **P0（M1）**；ST-10 仅保留心率区间等进阶占位 |

---

## 1. 背景与目标

### 1.1 背景

统计页承接母文档 §5.3 的 STA P0 能力，是"看懂自己"的首页深度页：首页概览卡（HP-04/05）点击、我的页「统计概览」（ME-03）进入，均为同一页面实现，避免两套逻辑。

### 1.2 目标

| 目标 | 验收口径 |
|---|---|
| 一眼看趋势 | 周期（今日/本周/本月）切换即出 次数/距离/时长/爬升 + 较上期对比；趋势有柱状/累积曲线与日历热力图辅助 |
| 与首页同口径 | 所有数值与首页概览、活动库来自同一聚合函数（STA-09/10），任何页面口径一致 |
| P0 全量覆盖 | STA-01/03/04/05（含 10/50/100 km 纪录）均在 M1 统计页交付 |
| 轻量可扩展 | 心率区间/训练负荷等进阶按 P1/P2 追加（ST-10 占位） |

### 1.3 Out of Scope

- 首页概览卡本身（HP-04，另见《首页 PRD》）；
- 训练负荷 / 功率曲线 / 心率区间（STA-06~08）→ P1/P2 追加，本期仅预留占位区；
- 目标设置与达成（"目标"占位，P2）；
- 年视图（整年日历/年汇总）与 Web 端统计（V1.x/V2 评估）。

---

## 2. 页面结构

```
│ ‹           统计                  │
│ [骑行进行中 …][回到记录]            │  ← 进行中时显示（复用横幅）
│ ┌──────────────────────────────┐  │
│ │ [今日] [本周] [本月]           │  │
│ │ ┌─────┬─────┐                │  │
│ │ │次数  │ 距离 │                │  │
│ │ │时长  │ 爬升 │                │  │ ← 周期指标格（ST-02）
│ │ └─────┴─────┘                │  │
│ │ 较上周同期 +12% · 目标待设(P2) │  │
│ └──────────────────────────────┘  │
│ 近 7 天                            │
│ ┌──────────────────────────────┐  │
│ │ ▁▃▂▅▁▇▂ 柱状（今日高亮）       │  │ ← ST-04
│ └──────────────────────────────┘  │
│ 周·月趋势                          │  ← ST-12（P0，M1）
│ ┌──────────────────────────────┐  │
│ │ [近8周] [近6月]               │  │
│ │ 柱状距离 + 累积折线            │  │
│ └──────────────────────────────┘  │
│ 日历热力图                         │  ← ST-11（P0，M1）
│ ┌──────────────────────────────┐  │
│ │ 一 二 三 四 五 六 日           │  │
│ │ ▢▢▢▣▢▢▢ …（橙阶热力）        │  │
│ └──────────────────────────────┘  │
│ 累计                              │  ← ST-05
│ 总里程 │ 总时长 │ 总爬升 │ 总次数      │
│ 个人纪录                          │  ← ST-06（含 10/50/100 km，P0）
│ 最远单次 / 单次最长 / 最大爬升 / 最快均速 │
│ 最快 10 km / 50 km / 100 km       │
└──────────────────────────────────┘
```

---

## 3. 功能需求（ST 清单，映射 STA）

| ID | 需求 | 说明 / 验收要点 | 优先级 |
|---|---|---|---|
| ST-01 | 周期切换 | 今日/本周/本月三段；默认本周；切换即时刷新全部区块；当前周期高亮（与首页 HP-04 分段同组件样式，独立状态） | P0 |
| ST-02 | 周期指标格 | 2×2：次数 / 距离 / 时长 / 爬升；无数据显示 `—`；口径=该周期内全部活动（含导入，STA-10） | P0 |
| ST-03 | 对比行 | 较上一周期同期的距离变化 ±% + 「目标功能待设」占位；无上一周期数据时显示"首次/首周"不显示百分比 | P0 |
| ST-04 | 近 7 天柱状图 | 最近 7 天（含今天）每日距离柱状，柱高按比例、今日高亮橙；无数据日零位灰柱；作为趋势视图的基础实现 | P0 |
| ST-05 | 累计区 | 总里程 / 总时长 / 总爬升 / 总次数（全部历史，含导入）；时长格式化（≥1h 用 `x.x h`，否则 `min`） | P0 |
| ST-06 | 个人纪录 | 从活动库计算：最远单次（km）、单次最长（时长）、最大爬升（m）、最快均速（km/h）；**并含按轨迹距离窗口计算的最快 10 / 50 / 100 km**（滚动切片求最短用时，对应 STA-04；距离不足显示 `—`）；每条标注出自活动，点击跳活动详情 | P0 |
| ST-07 | 数据变更联动 | 保存/导入/删除活动后回到统计页即最新；统计页为子页，数据源与首页共用聚合函数（实现期约束） | P0 |
| ST-08 | 返回 | 左上 ‹ 返回首页主视图（不改变 Tab 选中态；若由「我的」进入，返回统一到首页主视图） | P0 |
| ST-09 | 进行中横幅 | 会话进行中进入统计页：顶部显示橙色「骑行进行中」行，可一键回记录（复用 HP-12 组件与数据） | P0 |
| ST-10 | 进阶区块占位 | 预留「心率区间（STA-06）」「训练负荷（STA-08）」插入位（功率曲线随外设 M3 后评估）；未实现时隐藏不占位 | P1 |
| ST-11 | 日历热力图（STA-03） | 月视图按日热力：有骑行日按当日距离（或时长）映射橙阶色阶，无骑行空白；左右箭头切月份；顶部周期联动（本期默认跟随所选月/本期，可独立切月）；点击某日展示当日活动数摘要 | P0 |
| ST-12 | 周/月趋势与累积曲线（STA-05） | 「近 8 周 / 近 6 月」粒度切换：各周期距离柱状 + 累计距离折线叠加；数据按周/月聚合（口径同 ST-02）；今日/最新周期高亮 | P0 |

---

## 4. 空态 / 边界 / 异常

| 场景 | 行为 |
|---|---|
| 无任何活动 | 指标格/累计/纪录/热力图/趋势全空态（`—`/空白/零柱）；页内轻提示「完成骑行或导入历史数据后生成统计」 |
| 仅有导入活动 | 正常计入（来源不区分展示口径） |
| 删除全部活动 | 同上空态，数值即时归零 |
| 跨年/跨月切换 | 统计周期随当前系统日期计算；历史日期数据按本地时区归档（§4-5 主文档） |
| 数据不足（如仅 5 km 骑） | 最快 50/100 km 显示 `—`（ST-06 注明"距离不足"） |
| 数据量万级 | 聚合走缓存索引；柱图/热力/纪录计算 <500ms；不卡滚动 |
| 会话进行中 | 指标格不含进行中会话实时数据（仅含已保存活动）；实时数据在横幅呈现 |

---

## 5. 数据与口径（与首页概览共用）

- 统一聚合函数输入：`periodStart(周期) → {cnt, km, min, ele}`（实现期抽公共 service，首页 HP-04 与统计页 ST-02 必须同源）；
- 近 7 天 / 近 8 周 / 近 6 月：按自然日/周/月（本地时区）聚合；
- 最快 10/50/100 km：在活动轨迹上做滚动距离窗口切片，取最短用时（含导入轨迹；GPS 精度标记可过滤）；
- 个人纪录：最快均速 = max(km / (min/60))，要求 min>0；
- 单位制切换即时重算展示值（不改存储原值）。

---

## 6. 文案规范（初稿，✅ 可改）

| 位置 | 文案 |
|---|---|
| 页标题 | 统计 |
| 空态轻提示 | 完成骑行或导入历史数据后生成统计 |
| 对比占位 | 目标功能待设（P2） |
| 热力图空月 | 本月还没有骑行 |
| 窗口纪录不足 | 距离不足 |

---

## 7. 埋点（本地日志）

| 事件 | 触发 |
|---|---|
| stats_open | 进入统计（带来源：首页概览/我的） |
| stats_period_switch / stats_trend_switch | 周期切换 / 近8周·近6月切换 |
| stats_heat_month | 热力图月份切换 |
| stats_record_tap | 点击个人纪录跳详情 |

---

## 8. 视觉（沿用浅色主题 token）

- 周期分段与首页同组件（浅灰底、选中白底高亮）；
- 2×2 指标格与我的页总览卡同款（同一聚合、同一长相）；
- 柱状/折线：橙色渐变柱、灰零位柱、蓝灰累积折线；今日/最新周期标签深橙加粗；
- 热力图：7 列网格，色阶 `#fff → #ffe6d8 → #fc4c02`（按日强度）；有日点击反馈；
- 返回按钮 38×38 圆钮「‹」；页标题居中 17px。

---

## 9. 依赖与排期

| 依赖 | 归属 |
|---|---|
| 公共聚合函数（首页 HP-04 / 我的 ME-01 / 统计 ST-02/05/06 同源） | M1 |
| 图表基础能力（柱状/折线/热力均可本地自绘，性能自查） | M1 |
| 轨迹窗口切片算法（10/50/100 km PR） | M1 |
| 会话状态源（横幅复用） | M1 |

**里程碑**：M1 交付 ST-01~09/11/12（含热力图与周月趋势、窗口纪录）；ST-10 进阶区块 P1/P2 追加（随 STA-06/08 及外设 M3）。

---

## 10. 待确认清单（TBD）

| # | 问题 | 默认值 |
|---|---|---|
| TBD-1 | 近 7 天是否需要显示日均线/目标线 | 本期不显示 |
| TBD-2 | "最快均速"是否限制单次里程下限（防短骑刷榜） | ≥ 5 km 才参与计算（P1 再定细则） |
| TBD-3 | 柱状图交互（点击看精确值 vs tooltip） | M1 提供数值小标签，P1 增加点击弹层 |
| TBD-4 | 周期对比失败（无上一周期）显示 | 显示「首次/首周」文案，不显示百分比 |
| TBD-5 | 年视图（整年热力/年汇总）是否追加 | P1 评估，本期不做 |
| TBD-6 | 热力图强度口径：距离 vs 时长 vs 爬升 | 默认距离（色阶），设置可切换 |

---

## 11. 验收清单（QA 用例大纲）

- [ ] 首页概览卡与我的「统计概览」进入同一统计页；‹ 返回首页主视图（A-ST-01）
- [ ] 今日/本周/本月切换：指标格与对比行即时刷新，默认本周（A-ST-02）
- [ ] 同一数据集下：首页概览、统计指标格、我的总览三者数值一致（A-ST-03）
- [ ] 保存一次骑行后进入统计页：周期计数 +1、近 7 天/热力/趋势更新（A-ST-04）
- [ ] 删除/导入活动后统计即时正确（含导入计入默认）（A-ST-05）
- [ ] 日历热力图：按日色阶正确、切月可用、无骑行月空态（A-ST-06）
- [ ] 近 8 周/近 6 月趋势：柱状+累积折线与手动核对一致（A-ST-07）
- [ ] 最快 10/50/100 km：有轨迹数据时计算正确；距离不足显示 `—`（A-ST-08）
- [ ] 个人纪录数值与最大活动一致；点击跳详情（A-ST-09）
- [ ] 无数据时全 `—` + 空态轻提示；不崩溃（A-ST-10）
- [ ] 记录进行中进入统计：横幅显示并可回记录（A-ST-11）

---

*本 PRD 为 v1.1 可修改稿；ST-xx 可追溯主文档 STA-xx。改完可再做审计。*
