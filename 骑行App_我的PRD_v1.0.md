# 我的 PRD（跋涉 · 骑行 App）

## 0. 文档信息

| 项 | 内容 |
|---|---|
| 产品名 | 跋涉（暂定） |
| 文档版本 | v1.0（草稿，可修改稿） |
| 日期 | 2026-09-07 |
| 上游文档 | 《骑行App需求文档_v0.2.md》§5.7/5.8（ACC、设置）；§5.2（ACT-09 装备） |
| 关联文档 | 《骑行App_首页PRD_v1.1.md》（统计二级页同源）；《骑行App_记录PRD_v1.1.md》；《骑行App_统计PRD_v1.1.md》《骑行App_活动PRD_v1.1.md》（统计/活动入口） |
| 覆盖范围 | **我的 Tab**：资料（本地）、数据总览、功能入口（统计/装备/导入/导出/设置/关于） |
| 原型对应 | prototype v1.4 的「我的」页 |

---

## 1. 背景与目标

### 1.1 背景

本地优先模式下「我的」是**个人数据中心与功能总开关**：看累计、管理数据（导入/导出）、进设置。账号/云端（V2）之后此页演进为账号页，本期按本地模式设计并预留字段。

### 1.2 目标

| 目标 | 验收口径 |
|---|---|
| 累计一眼可见 | 总里程/总时长/总爬升/总次数实时正确 |
| 功能有明确出口 | 统计/装备/导入/导出/设置均可进入且状态一致 |
| 本地模式可自解释 | 无需注册即可用；页面明确标注"本地模式" |
| 数据可带出 | 导出全部数据（PIPL 可携带性预留，IO-05） |

### 1.3 Out of Scope

- 账号注册/登录/云同步（ACC-03~06）→ V2；
- 设置项的完整落地（采样档/后台权限引导等）——本 PRD 只定义清单与跳转目标，实现随对应模块；
- 装备 CRUD 完整功能（ACT-09）→ P2；
- 深色模式、多主题。

---

## 2. 页面结构

```
│ (跋) 本地骑行者            │
│      本地模式 · 无需登录 · V2 再账号化 │
│ ┌──────────────────────────┐  │
│ │ 总里程 1,256 km │ 总时长 86.2h │  │ ← 数据总览（ME-01，实时）
│ │ 总爬升 21,400m │ 总次数 98     │  │
│ └──────────────────────────┘  │
│ 功能入口                       │
│ 📊 统计概览            ›  →统计页│
│ 🚲 我的装备  (0 辆车)    ›      │
│ 📥 导入骑行数据         ›       │
│ 📤 导出数据             ›       │
│ ⚙️ 设置                ›       │
│ ℹ️ 关于                 ›       │
├────────────────────────────────┤
│  首页   地图   记录   我的         │ ← Tab
└────────────────────────────────┘
```

---

## 3. 功能需求（ME 清单）

| ID | 需求 | 说明 / 验收要点 | 优先级 |
|---|---|---|---|
| ME-01 | 数据总览 | 2×2：总里程/总时长/总爬升/总次数；口径=全部本地活动（含导入，STA-10）；每次进入与数据变更后刷新；无数据时四项 `—` | P0 |
| ME-02 | 本地资料 | 头像（本地图库选图 ✅ 或默认「跋」字）、昵称编辑（默认"本地骑行者"）；仅存本地（ACC-02）；体重/性别可选（用于千卡与心率区间，ACC-02） | P1 |
| ME-03 | 统计概览入口 | 跳首页 Tab 内的统计二级页（openStats，与首页概览卡同页同源） | P0 |
| ME-04 | 我的装备 | 列表入口：展示车辆数量（0 辆 → 子文案"添加你的第一辆车"）；进装备页（P2，字段：名称/类型/重量/里程累计）；活动关联装备后自动累计里程（ACT-09） | P2 |
| ME-05 | 导入骑行数据（IO-01~06） | 文件选择（.fit/.gpx/.tcx 多选）→ 解析 → 预览（条数/字段缺失说明）→ 去重提示（IO-03）→ 导入完成 Toast + 全页刷新；失败给具体原因（IO-06）；导入活动参与统计/路线库（默认计入，可勾选 STA-10） | P1 |
| ME-06 | 导出数据（IO-05） | 单条导出（活动详情内）与**导出全部**：打包 .gpx/.fit 或原始库文件（格式 ✅ 待定），走系统分享面板；进度与结果反馈；对应 PIPL 可携带性 | P1 |
| ME-07 | 设置（清单见 §4） | 进入设置页；各条目跳转/即时开关；「后台与权限引导」在记录首次需要时也主动出现 | P1 |
| ME-08 | 关于 | 版本号、原型日期、隐私政策占位（上云前必备）、开源致谢（如有） | P1 |
| ME-09 | 数据管理（安全区） | 清除全部本地数据（二次确认）→ 回到首启空态；V2 增加注销/云端删除（ACC-06） | P2 |
| ME-10 | 单位/本地化入口 | 快速改单位制（km/mi、kg/lb）→ 全局即时换算（§4-4 主文档）；语言入口预留（i18n） | P1 |

---

## 4. 设置项清单（映射主文档 §5.8，点击即达的项先立项）

| 分组 | 项 | 类型 | 说明 / 跳转 | 归属 |
|---|---|---|---|---|
| 通用 | 单位制 | 即时开关 | km/mi、kg/lb，全局换算 | P1 |
| | 语言 | 占位 | 仅中文，预留 | P2 |
| 记录 | 采样与省电 | 选择 | 1s/5s/10s 档 + 精度档（默认 1s 高质量） | P1 |
| | 自动暂停 | 开关 | 与记录准备页开关同步（同一数据源） | P0 |
| | 自动计圈 | 开关+阈值 | 关/1/5/10/25km（默认关） | P0 |
| | 屏幕常亮 | 开关 | 记录中常亮（默认开） | P1 |
| 地图 | 底图偏好 | 选择 | 默认高德（标准/卫星记忆已在 MP-01） | P1 |
| 权限 | 后台与权限引导 | 跳转 | 图文引导：自启动/后台运行/精确定位（小米/华为/OPPO/vivo 定制文案） | P1 |
| 通知 | 通知权限 | 跳转 | 记录中常驻通知用途说明 | P2 |
| 存储 | 存储占用/清理 | 跳转 | 轨迹压缩占用、离线包占用、清理（保留活动数据） | P2 |
| 关于 | 版本/隐私 | 跳转 | ME-08 | P1 |

> 原则：设置项不做重复入口（记录页开关与设置页同一数据源同步）。

---

## 5. 空态 / 边界 / 异常

| 场景 | 行为 |
|---|---|
| 无任何数据 | 总览四项 `—`；其余入口可用（导入可先灌数据） |
| 导入大文件/多文件 | 显示进度与可取消；解析失败单条跳过并列出原因（IO-06 整批 vs 跳过口径） |
| 导出无数据 | 按钮置灰/Toast「暂无可导出数据」 |
| 清除全部数据（P2） | 强提示二次确认 + 需输入"确认"字样；完成后回首启空态 |
| 单位切换 | 当前页与所有页即时重算（事件广播，不改存储原值） |
| 版本升级数据迁移 | 本地库版本化迁移；失败提示并保留备份 |

---

## 6. 数据与字段（增量）

- UserSettings：nickname、avatar(本地 path)、gender/birthYear(可选)、unit(km/mi)、weightKg、defaultType、autoPause、autoLapKm、mapEngine/mapStyle、sampling、screenOn 等 —— 本地存储 + 未来云端（User 字段预留）。
- 导出包格式：`.gpx/.fit` 逐条 + `activities.json`（元数据）✅ 待定。

---

## 7. 文案规范（初稿，✅ 可改）

| 位置 | 文案 |
|---|---|
| 副标题 | 本地模式 · 无需登录 · V2 再账号化 |
| 装备空态 | 添加你的第一辆车 |
| 导入完成 | 已导入 {n} 条骑行（{m} 条重复已跳过） |
| 清除数据按钮 | 清除全部本地数据（不可恢复） |
| 统计入口 | 统计概览 |

---

## 8. 埋点（本地日志）

| 事件 | 触发 |
|---|---|
| me_pv / me_stats_open | 进入我的 / 打开统计 |
| import_done / import_fail | 导入完成/失败（含条数） |
| export_all | 导出全部 |
| unit_switch | 切换单位 |
| setting_open_{key} | 打开各设置项 |

---

## 9. 视觉（沿用浅色主题 token）

- 头像 58px 圆形浅灰渐变；昵称 16px；
- 总览卡沿用 2×2 分隔格（同统计页样式，保证口径视觉一致）；
- 功能入口列表沿用行样式（34px 图标圆角块 + 文字 + ›）；
- 底部 Tab 文字样式与全局一致。

---

## 10. 依赖与排期

| 依赖 | 归属 |
|---|---|
| 本地数据聚合（总览/统计同源函数） | M1 |
| 设置数据源（与记录准备页开关/首页分段同步） | M1 |
| 文件解析器（.fit/.gpx/.tcx） | M2（GPX 先行） |
| 系统分享/文件导出 | M2 |
| 装备/账号（V2 模型预留） | P2 |

**里程碑**：M1 交付 ME-01/03 + 设置数据源同步；M2 交付导入/导出（IO）；ME-02/07/08/10 → V1.x；装备与账号 V2。

---

## 11. 待确认清单（TBD）

| # | 问题 | 默认值 |
|---|---|---|
| TBD-1 | 总时长展示口径（分钟 vs h） | 页面用可读格式（如 86.2 h）；统计页同 |
| TBD-2 | 头像来源（系统图库权限成本） | P1 再做，默认字头像 |
| TBD-3 | 导出包格式与粒度 | `.gpx/.fit` 逐条 + 元数据 JSON（V2 前可调整） |
| TBD-4 | 导入后是否默认计入统计 | 默认计入，可在导入预览勾选（STA-10） |
| TBD-5 | 体重/性别是否必填 | 均可选；千卡用默认 65kg 兜底 |
| TBD-6 | 清除数据是否需要"确认"字样输入 | 需要（防误触） |

---

## 12. 验收清单（QA 用例大纲）

- [ ] 首次进入：四项为 `—`；入口齐全无死链（A-ME-01）
- [ ] 完成一次骑行保存后回我的：总览四项正确 +1（A-ME-02）
- [ ] 统计概览：从我的进入的统计页与首页进入的为同一实现、返回路径正确（A-ME-03）
- [ ] 导入 1 条 .gpx：出现在总览/最近活动/统计，且来源标记"导入"（A-ME-04）
- [ ] 重复导入同一文件：提示跳过，数据不翻倍（A-ME-05）
- [ ] 导出全部：产出文件可用（可用其他软件打开）；无数据时不可导出（A-ME-06）
- [ ] 单位制切换后我的/首页/统计数值即时换算（A-ME-07）
- [ ] 设置页与记录准备页的开关双向同步（A-ME-08）

---

*本 PRD 为 v1.0 可修改稿；ME-xx 可追溯主文档 ACC/IO/ACT 编号。改完可再做审计。*
