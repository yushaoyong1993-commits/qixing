# 活动 PRD（跋涉 · 骑行 App）——列表页 + 详情页

## 0. 文档信息

| 项 | 内容 |
|---|---|
| 产品名 | 跋涉（暂定） |
| 文档版本 | v1.1（里程碑粒度对齐稿） |
| 日期 | 2026-09-07 |
| 上游文档 | 《骑行App需求文档_v0.3.md》§5.2（ACT-01~09）；§5.3（STA 口径） |
| 关联文档 | 《骑行App_首页PRD_v1.1.md》（入口 HP-07/09/10）；《骑行App_统计PRD_v1.1.md》（个人纪录跳详情 ST-06）；《骑行App_地图路线PRD_v1.1.md》（活动转存路线 MP-13）；《骑行App_我的PRD_v1.0.md》（导入/导出 IO）；《需求一致性审计报告2_2026-09-07》（Q-M2-01） |
| 覆盖范围 | **活动列表页**（二级）+ **活动详情页**（二级）：浏览/筛选/进入详情、详情指标/地图/曲线/分段/编辑/删除/导出/转存路线 |
| 原型对应 | prototype v1.5 的「活动列表/详情」子页 |
| 修订记录 | v1.0：首稿。v1.1：审计对齐——AD-06 操作区内里程碑粒度拆分（编辑/删除=M1；保存为路线 MP-13、导出 GPX IO-05 = M2）；AL-07 长按快捷中导出项注明 M2；首页/我的引用措辞同步 |

---

## 1. 背景与目标

### 1.1 背景

活动（Activity）是骑行数据的归档形态。首页「最近活动/上次轨迹」、统计个人纪录、导入数据最终都指向活动列表/详情。本 PRD 补齐当前导航里唯一"待设计"的死路。

### 1.2 目标

| 目标 | 验收口径 |
|---|---|
| 快速找到一次骑 | 时间倒序 + 年月/类型筛选；万条数据流畅 |
| 一次骑行完整复盘 | 指标卡 + 地图轨迹 + 海拔/速度曲线 + 分圈分段 |
| 活动可管理 | 编辑名称类型/删除（二次确认）/导出/转存路线，入口清晰 |
| 口径全局一致 | 数值与首页概览/统计同源聚合（STA-09/10）；曲线与统计图表同一计算函数 |

### 1.3 Out of Scope

- 活动云端同步/跨端（V2）；
- 社交分享单条活动到动态流（V3）；
- 功率曲线/训练负荷等进阶图表（STA-07/08，P2，详情页预留区块）；
- 活动的批量管理（批量导出在 IO-05 覆盖；批量删除不做）。

---

## 2. 页面结构

**列表页（二级，从首页/我的进入）**：

```
│ ‹            活动            [＋手动记录]│  ← 手动补记入口（附录A-7，P1）
│ [全部] [公路] [山地] [通勤] [训练] [导入] │  ← 类型/来源筛选 chips
│ [年▾]   [月▾]                          │  ← 时间筛选（AL-02）
│ 2026年9月                              │  ← 月份分组头（折叠）
│ 🚴 晨骑 · 9月7日  42.3km  ↑640m  3:12:45 │
│ 📥 通勤(导入) 9月6日 12.1km ↑40m  38:00  │
│ ……（虚拟滚动）                          │
├────────────────────────────────────────┤
│ 底部 Tab Bar（当前 Tab 保持：首页/我的）     │
```

**详情页（二级，从列表/首页缩略/统计纪录/摘要进入）**：

```
│ ‹            骑行详情          [编辑]    │
│ 晨骑                          [公路]     │
│ 2026-09-07 06:42 · 本地 · 手机记录        │
│ ┌──────────────────────────────┐       │
│ │  距离   时长    移动时长  均速   │       │ ← 核心指标卡
│ └──────────────────────────────┘       │
│  最大速度 48.6 · 累计爬升 640 · 下降 635   │
│ ┌──────────────────────────────┐       │
│ │    路线地图（复用轨迹渲染，可全屏） │       │
│ └──────────────────────────────┘       │
│ [海拔剖面] [速度] [心率]  [时间轴|距离轴]  │ ← 叠加曲线（AD-04）
│ 分段（每公里 / 计圈 L1..L5）              │
│ ┌────────────────────────────────────┐  │
│ │ 🚴 保存为路线(M2)  📤 导出 GPX(M2)   │  │ ← AD-06（M2 项 M2 前置灰）
│ │ ✏️ 编辑(M1)          🗑 删除(M1)     │  │
│ └────────────────────────────────────┘  │
```

---

## 3. 功能需求

### 3.1 活动列表页（AL 清单）

| ID | 需求 | 说明 / 验收要点 | 优先级 |
|---|---|---|---|
| AL-01 | 列表浏览 | 时间倒序；每条：来源图标(手机/导入/手环) + 名称 + 日期 + 距离 + 时长 + 爬升 + 均速；名称过长省略 | P0 |
| AL-02 | 时间筛选 | 顶部年/月下拉；可选"仅看今年/本月/近30天"快捷项（✅ 默认全部） | P0 |
| AL-03 | 类型/来源筛选 | chips：全部/公路/山地/通勤/训练/导入（来源与类型可叠加，来源筛选 P1） | P0（类型）/P1（来源） |
| AL-04 | 分组头 | 按月分组展示「2026年9月」折叠可展开；组内日期倒序 | P1 |
| AL-05 | 空态 | 无活动：引导卡「开始你的第一次骑行 / 导入历史数据」（同首页 HP-11 文案与行为） | P0 |
| AL-06 | 行点击 | → 活动详情（AD） | P0 |
| AL-07 | 长按快捷 | 长按行 → 操作单：编辑 / 删除（二次确认）/ 导出 GPX（**导出随 IO-05，M2 前置灰**） | P1 |
| AL-08 | 手动补记入口 | 页头「＋手动记录」：无 GPS 手动录入 时间/距离/时长/备注（附录 A-7，P1 立项） | P1 |
| AL-09 | 性能 | 万条活动流畅滚动（虚拟化/分页 + 索引）；缩略指标懒计算 | P0 |
| AL-10 | 返回与 Tab 保持 | 列表作为二级页：‹ 返回上一来源（首页主视图/我的页）；底部 Tab 选中态不跳变 | P0 |

### 3.2 活动详情页（AD 清单）

| ID | 需求 | 说明 / 验收要点 | 优先级 |
|---|---|---|---|
| AD-01 | 头部信息 | 名称、类型 tag、日期时间、来源（手机/导入，含导入文件名）、天气（RCD-13，P2）；「编辑」入口 | P0 |
| AD-02 | 核心指标卡 | 距离/时长/移动时长/均速/最大速度/累计爬升与下降/平均与最大心率/千卡（有外设时心率、功率另区展示）；口径与统计一致（STA-09） | P0 |
| AD-03 | 路线地图 | 复用全局轨迹渲染（地图 PRD MP-04）；底图切换标准/卫星；点击放大全屏（二次弹层）；渲染失败降级文字占位 | P0 |
| AD-04 | 海拔/速度曲线 | 可交互曲线：海拔为基线、速度/心率叠加开关；时间轴/距离轴切换；拖动查看对应点数值（实现期定图表库或自绘） | P0 |
| AD-05 | 分段明细 | 每公里配速表（含爬升）；自动/手动计圈分段（ACT-04，P1）；缺段显示 `—` | P1 |
| AD-06 | 操作区 | **M1（P0）**：编辑（ACT-05）、删除（ACT-06，二次确认后回列表刷新）；**M2（随相关 PRD）**：保存为路线（MP-13）、导出 GPX（IO-05）——M2 项在 M2 前置灰并提示 | P0 |
| AD-07 | 数据缺失容错 | 无心率/踏频/功率：指标卡对应项显示 `—` 并提示"未连接外设或来源文件缺失"（ACT-07）；轨迹文件缺字段不崩溃 | P0 |
| AD-08 | 纪录高亮进入 | 从统计个人纪录跳入时：顶部 toast「刷新了个人纪录 🎉」+ 对应纪录值高亮（ST-06 联动，原型 v1.5 已演示） | P1 |
| AD-09 | 预留区块 | 功率曲线（STA-07）、照片（ACT-08）、装备关联（ACT-09）——有数据才展示 | P2 |

---

## 4. 空态 / 边界 / 异常

| 场景 | 行为 |
|---|---|
| 活动被并发删除 | 进入详情前校验：不存在 → Toast「该活动不存在」并返回列表刷新 |
| 轨迹点缺失/损坏（导入文件） | 详情仍打开：地图区显示"轨迹缺失"，指标卡给出可用字段 |
| 删除 | 二次确认 + 明确后果（本地不可恢复）；删除后列表/首页/统计同步刷新（数据源单一） |
| 列表超长 | 虚拟滚动；筛选时重置到顶部 |
| 编辑名称/类型 | 保存后列表、首页最近活动同步；统计按新类型重新归类（类型筛选生效） |
| 导出失败 | Toast 具体原因；大文件走系统分享面板 |
| 详情页数据与记录进行中 | 详情只读已保存活动；与进行中会话无耦合 |

---

## 5. 跨页契约

| 入口来源 | 到达 | 参数/行为 |
|---|---|---|
| 首页最近活动行 / 上次轨迹缩略 | 活动详情 | activityId（首页 HP-07/09） |
| 首页「全部」 | 活动列表（默认本年度倒序） | —（HP-10） |
| 我的「全部活动」 | 活动列表 | —（原型 v1.5 已加） |
| 统计个人纪录行 | 活动详情 + 纪录高亮 | activityId + recordType（ST-06/AD-08） |
| 我的-导入完成 | 回列表（或提示可查看新导入） | —（IO-01） |
| 记录摘要「保存」后 | 首页（可后续进详情） | — |
| 详情「保存为路线」（M2） | 地图页路线库 | activityId（MP-13） |

---

## 6. 文案规范（初稿，✅ 可改）

| 位置 | 文案 |
|---|---|
| 页标题 | 活动 / 骑行详情 |
| 手动补记 | ＋手动记录 |
| 删除确认 | 删除后不可恢复，确定删除？ |
| 纪录高亮 | 刷新了个人纪录 🎉 |
| 缺心率提示 | 未连接外设或来源文件缺失 |
| M2 前置灰 | 该功能将在后续版本开放（M2） |

---

## 7. 埋点（本地日志）

| 事件 | 触发 |
|---|---|
| act_list_pv / act_filter | 进入列表 / 筛选变化 |
| act_open | 打开详情（带来源） |
| ad_edit / ad_delete / ad_export / ad_save_route | 各操作 |
| ad_chart_axis | 曲线轴切换 |

---

## 8. 视觉（沿用浅色主题 token）

- 列表行同首页最近活动样式（40px 圆角图标块 + 主/次文案 + 右侧数值）；来源"导入"用浅底+灰图标；
- chips 与地图页/记录页风格一致；
- 详情：指标卡用 2×2/行列混合（复用 grid2/cell 样式）；曲线区卡片化，图例颜色：海拔=橙、速度=蓝、心率=红（✅ 待定）；
- 地图/全屏弹层复用 overlay；删除按钮红色语义色。

---

## 9. 依赖与排期

| 依赖 | 归属 |
|---|---|
| 公共聚合（列表缩略、指标、曲线同源） | M1 |
| 全局轨迹渲染（详情地图/全屏） | M1（复用地图页） |
| 图表基础能力 | M1（AD-04） |
| 文件导出（IO-05）/ 转存路线（MP-13） | M2 |
| 手动补记（AL-08） | V1.x |

**里程碑**：M1 交付 AL-01~06/09/10 + AD-01~04/06(M1 部分)/07（跟随"活动管理 P0"）；AL-04/07、AD-05、AD-08 → V1.x；AD-06 的 M2 子操作（保存路线/导出）随地图 IO/MP 里程碑；AD-09 随 STA/装备 P2。

---

## 10. 待确认清单（TBD）

| # | 问题 | 默认值 |
|---|---|---|
| TBD-1 | 列表分组粒度：月分组 vs 扁平倒序 | 月分组（AL-04）默认开 |
| TBD-2 | 曲线"距离轴"是否需要重采样对齐（不同采样率的导入文件） | 统一重采样到 1s/50m 网格 |
| TBD-3 | 均速 vs 移动均速展示主次 | 主显均速，副显移动均速（同母文档 ACT-02） |
| TBD-4 | 手动补记是否计入统计与纪录 | 计入，标记"手动" |
| TBD-5 | 详情页是否需要"编辑轨迹裁剪起终点" | 不做（P3 评估） |

---

## 11. 验收清单（QA 用例大纲）

- [ ] 首页「全部」/ 最近活动 / 上次缩略分别到达列表 / 详情，返回路径正确（A-AL-01）
- [ ] 年月/类型筛选生效；分组可折叠；万条数据滚动流畅（A-AL-02）
- [ ] 空态引导可用；无活动时进入列表不崩溃（A-AL-03）
- [ ] 详情指标卡与首页/统计同源一致（同一活动三处数值相同）（A-AD-01）
- [ ] 地图渲染与路线页一致；全屏可切换底图（A-AD-02）
- [ ] 海拔/速度曲线交互正确（拖拽取点、轴切换）（A-AD-03）
- [ ] 编辑保存后列表/首页同步；删除二次确认且全端刷新（A-AD-04）
- [ ] 导入缺心率文件：详情 `—` + 提示，不崩溃（A-AD-05）
- [ ] M2 前"保存为路线/导出 GPX"置灰提示；M2 后导出文件可被其他软件打开（A-AD-06）

---

*本 PRD 为 v1.1 可修改稿；AL/AD-xx 可追溯主文档 ACT-xx。改完可再做审计。*
