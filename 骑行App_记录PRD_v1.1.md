# 记录 PRD（跋涉 · 骑行 App）

## 0. 文档信息

| 项 | 内容 |
|---|---|
| 产品名 | 跋涉（暂定） |
| 文档版本 | v1.1（审计对齐稿） |
| 日期 | 2026-09-07 |
| 上游文档 | 《骑行App需求文档_v0.3.md》§5.1（RCD-01~14） |
| 关联文档 | 《骑行App_首页PRD_v1.1.md》（§7 跨 Tab 契约）；《骑行App_地图路线PRD_v1.1.md》（路线预载）；《需求一致性审计报告_2026-09-07》 |
| 覆盖范围 | **记录 Tab**：准备页 → 记录中（实时页）→ 结束摘要 → 后台/中断/草稿全状态机 |
| 原型对应 | prototype v1.4 的「记录」页 |
| 修订记录 | v1.0：首稿（草稿）。v1.1：审计对齐——RC-21 拆分（语音播报=语音 P2，震动/提示音归 RCD-10 P1）；RC-12 摘要可展开改名/备注；RC-04 明确 GPS 预热于进入准备页开始；§6 补充首页长按（HP-03）交接行 |

---

## 1. 背景与目标

### 1.1 背景

记录 Tab 是全部骑行数据的**源头与会话宿主**：首页「开始骑行」、地图「沿此路线骑行」最终都落到这里。核心难点不在 UI 而在**状态机与可靠性**（GPS/后台/被杀/外设）。

### 1.2 目标

| 目标 | 验收口径 |
|---|---|
| 一次点击开始 | 任何入口到达后，用户点「开始」即进入记录，无多余步骤 |
| 数据可靠不丢 | 后台/锁屏/来电/被系统杀进程，轨迹与数据不丢（RCD-06/07） |
| 实时信息清晰 | 6 项核心指标实时跳动 + 轨迹生长，暂停/计圈状态一眼可辨 |
| 结束可决策 | 保存 / 再骑一段 / 放弃三条路清晰，误触有二次确认 |

### 1.3 Out of Scope（本 PRD 不做）

- 外设硬件接入（BLE/心率带等）→ M3 专项（预留数据槽与来源标记，见 §5 RCD）；
- **完整导航 M2** 的 turn-by-turn 语音/偏航重规划（M1 先做"沿路线方向/距离轻提示"，RC-17）；
- 自动暂停的复杂滤波（速度≈0 判定参数）——实现期细调；
- 多会话、双设备同录（同一时刻全 App 仅允许一个会话）。

---

## 2. 会话状态机（核心）

```
        ┌──────────┐  点「开始」   ┌──────────┐   手动/自动暂停   ┌──────────┐
        │  准备页    │ ──────────▶ │ 记录中    │ ──────────────▶ │  已暂停   │
        │ (Idle)   │              │(Recording)│ ◀────────────── │ (Paused) │
        └──────────┘              └──────────┘     继续        └──────────┘
             ▲                          │ 点「结束」
             │ 回到准备页                ▼
        ┌────┴────┐              ┌──────────────┐
        │  草稿    │  恢复继续    │ 结束·摘要      │ ──保存──▶ 写入活动库 → 回首页刷新
        └─────────┘ ◀────────── │  (Summary)   │ ──再骑一段──▶ 回记录中(取消暂停)
                                └──────────────┘
```

**规则（硬约束）**：
1. 全 App **同一时刻最多一个进行中会话**（首页/地图/记录任何"开始"入口在会话中都被拦截并引导）；
2. 会话与 Tab 解耦：记录中可自由切 Tab，会话在后台继续（常驻通知可回记录）；
3. 异常退出（进程被杀/来电强制）→ 生成**草稿**（RCD-07），下次启动首页可见并可「继续/保存/丢弃」；
4. 时长口径：暂停不计时（移动时长），保存后同时记录总时长与移动时长（ACT 字段）。

---

## 3. 页面结构

**准备页（Idle）**：

```
│ 开始新骑行                ● GPS 已就绪 │
│ [公路] [山地] [通勤] [训练]             │ ← 类型（默认沿用上次）
│ ┌──────────────────────────────┐   │
│ │ 自动暂停         [开关 ON]     │   │ ← 默认开（RCD-04 v0.3）
│ │ 自动计圈(5km)   [开关 OFF]     │   │ ← 默认关（RCD-09 v0.3）
│ └──────────────────────────────┘   │
│ [ 🚴 开始骑行 ]（橙色主按钮）          │
│ GPS 轨迹 · 后台锁屏持续记录 · 外设 M3  │
```

**记录中（Recording / Paused）**：

```
│ [公路] [L1]                (暂停→琥珀色) │
│            00:42:18                    │  ← 大计时（暂停不跳）
│        记录中，切走不中断 · 自动暂停已开启 │
│ ┌──────────────────────────────┐     │
│ │  轨迹地图（白描边橙线实时生长）   │ REC ● │
│ └──────────────────────────────┘     │
│ 距离    当前速   均速   │ 心率  爬升  千卡 │  ← 2×3
│ [计圈]   [■结束]   [⏸暂停]            │
│            放弃本次骑行（二次确认）        │
```

**结束·摘要（全屏 overlay）**：名称（默认名，可展开改名/备注）、距离/时长/均速/爬升/平均心率/计圈数 → `保存` / `再骑一段`。

---

## 4. 功能需求（RC 清单，映射 RCD）

| ID | 需求 | 说明 / 验收要点 | 优先级 |
|---|---|---|---|
| RC-01 | 准备页-类型 | 公路/山地/通勤/训练 单选；默认沿用上次选择（UserSettings.defaultType）；进入会话后不可改（"改类型"为 P2） | P0 |
| RC-02 | 自动暂停开关 | **默认开（✅，v0.3 决策）**；速度≈0 持续 N 秒判定（参数实现期校准）；暂停期间计时停止、地图静止 | P0 |
| RC-03 | 自动计圈开关 | **默认关（✅，v0.3 决策）**；开则每 5 km 自动计一圈（阈值可设 1/5/10/25，§设置）；圈数实时显示 L1/L2… | P0 |
| RC-04 | 开始记录 | 进入准备页即启动 **GPS 预热**并展示信号状态（GPS 已就绪/定位中…/信号弱）；点「开始」：①校验定位权限（未授权→解释弹窗流程 HP-15 同款）→②信号未就绪则提示等待（<5s 转就绪，超时降级提示）→③进入记录中 | P0 |
| RC-05 | 实时指标 | 6 格：距离、当前速度、均速、心率、爬升、千卡；心率无外设时显示 `—`（来源标记，DEV-04）；数值单位随用户设置 | P0 |
| RC-06 | 轨迹实时绘制 | 自绘轨迹线随骑行生长（复用地图页渲染函数）；当前位置圆点 | P0 |
| RC-07 | 暂停/继续 | 圆钮切换：暂停→琥珀色+「Ⅱ 已暂停」+计时停；继续→恢复 | P0 |
| RC-08 | 手动计圈 | 计圈按钮 → 圈数+1 + Toast `已计圈 L{n} @ {km}`；计圈不影响暂停状态 | P0 |
| RC-09 | 结束 | 停止计时 → 摘要 overlay（RC-12） | P0 |
| RC-10 | 放弃（二次确认） | 点「放弃本次骑行」→ 文案变「再点一次确认放弃」（3s 复位）→ 二次点击丢弃会话，回准备页 | P0 |
| RC-11 | 后台/锁屏记录 | 后台与锁屏持续记录；常驻通知显示 `骑行进行中 00:42:18 · 12.3 km`，点击回记录页；iOS 需后台定位用途声明（§9 主文档） | P0 |
| RC-12 | 摘要-保存 | 摘要字段如上；**默认名直存，可展开改名/备注**；保存→写活动库→通知首页刷新（HP-14）→回首页；默认名=`{类型} {时段}骑行`（时段：晨/午间/晚间，附录 A-3） | P0 |
| RC-13 | 摘要-再骑一段 | 关闭摘要恢复记录（取消暂停，计时继续） | P0 |
| RC-14 | 草稿恢复（RCD-07） | 启动检测未完成草稿：首页提示条「继续/保存/丢弃」；继续→恢复会话续录；保存→按已有数据入库；丢弃→二次确认删除 | P0 |
| RC-15 | 防双会话 | 所有开始入口统一校验：会话中→Toast「请先结束当前骑行」+ 跳记录页 | P0 |
| RC-16 | GPS 丢失处理 | 定位连续丢失 >30s：横幅提示「GPS 信号弱」；恢复自动续录；丢失段标记（活动详情可查看） | P1 |
| RC-17 | 沿路线骑行（轻导航） | 从地图路线预载（routeId）：记录中顶部显示 `沿「xx」· 剩 18.2 km` 方向/距离提示（轻量，不依赖在线规划）；M2 升级完整导航 | P0(M1)/M2 增强 |
| RC-18 | 外设数据槽 | 记录中预留：外设连接状态 + 心率/踏频/功率实时数据接入点（M3 实现）；本期心率显示 `—` 并标注"未连接" | P1 |
| RC-19 | 屏幕常亮/防误触 | 记录中默认屏幕常亮（可关，设置）；结束/放弃均需确认，其余区域无破坏性按钮 | P1 |
| RC-20 | 长距离容错 | >8h：轨迹点压缩策略、内存水位告警、电量低提示（≤20% 提醒）；存储估算 | P1 |
| RC-21 | 语音播报（轻） | 开始/暂停/计圈/到点可选**语音播报**（✅ 默认关）；**震动与按钮提示音归 RCD-10（P1）**，与语音分开计量 | P2 |

---

## 5. 空态 / 边界 / 异常

| 场景 | 行为 |
|---|---|
| 定位被拒后点开始 | 解释弹窗 +「去设置」；授权回来自动继续流程；不静默失败 |
| GPS 未收敛 | 状态行「定位中…」；超时仍记录（位置标记低精度）并提示 |
| 来电/切走 | 会话继续（后台记录）；回前台横幅/通知即时可达 |
| 进程被杀 | 生成草稿（RC-14）；不出现"半条轨迹"（事务写入，§6 主文档可靠性） |
| 存储满 | 阻止新记录并提示清理（IO/清理入口） |
| 会话中打开摘要再被杀 | 摘要态不持久；重启后按草稿处理（数据已分段缓存） |
| 自动暂停与手动暂停叠加 | 手动暂停优先；均恢复后重新计时 |

---

## 6. 跨页契约（反向引用）

| 事件 | 本页行为 | 对方页 |
|---|---|---|
| 首页「开始骑行」/空态按钮 | 进入准备页（不自动开录，用户再点开始） | 首页 PRD §7 |
| 首页长按「开始骑行」选路线（HP-03，P1） | 预载 routeId → 同「沿此路线骑行」流程 | 首页 HP-03 |
| 首页进行中横幅「回到记录」 | 直达记录实时页 | 首页 HP-12 |
| 首页草稿「继续」 | 恢复会话 | 首页 HP-13 |
| 地图「沿此路线骑行」 | 预载 routeId → 准备页显示路线条 → 开始后进入轻导航 | 地图 PRD §6 |
| 保存成功 | 广播数据变更 → 首页概览/最近活动刷新 | 首页 HP-14 |

---

## 7. 文案规范（初稿，✅ 可改）

| 位置 | 文案 |
|---|---|
| 准备页标题 | 开始新骑行 |
| GPS 状态 | GPS 已就绪 / 定位中… / GPS 信号弱 |
| 主按钮 | 开始骑行 |
| 放弃文案 | 放弃本次骑行 → 再点一次确认放弃 |
| 摘要标题 | 骑行完成 🎉 |
| 再骑一段 | 再骑一段 |
| 外设心率未连 | — |
| 轻导航条 | 沿「{路线}」骑行 · 剩 {km} |

---

## 8. 埋点（本地日志）

| 事件 | 触发 |
|---|---|
| rec_start / rec_pause / rec_resume | 会话状态变化 |
| rec_lap | 计圈（自动/手动带来源） |
| rec_finish_save / rec_finish_discard | 保存/放弃 |
| rec_draft_restore / rec_draft_save / rec_draft_discard | 草稿处理 |
| gps_loss / gps_recover | 信号丢失/恢复 |

---

## 9. 视觉（沿用浅色主题 token）

- 计时器 54px 800 字重等宽；暂停态主区不置灰（保留现场）；
- 状态色：记录=橙红 REC、暂停=琥珀、放弃二次确认=红；
- 主按钮橙渐变（与首页 Hero 一致）；底部三圆钮：计圈(浅灰)、结束(橙 74px)、暂停(琥珀)；
- 记录中顶部条显示类型+圈数小标签，保证锁屏回来可读；
- 数字等宽字体（tabular-nums）。

---

## 10. 依赖与排期

| 依赖 | 归属 |
|---|---|
| 定位服务与后台能力（iOS 声明/Android 前台服务 type=location） | M0（骨架） |
| 本地存储 + 事务写入（活动/轨迹原子） | M0~M1 |
| 轨迹自绘渲染（与地图页共用） | M1 |
| 草稿机制 | M1 |
| 会话状态机（贯穿记录/首页横幅/地图入口） | M1 |
| 外设（BLE + HealthKit/Health Connect） | M3（DEV-01~08，真机后延） |

**里程碑**：M1 交付 RC-01~15/17（记录闭环+轻导航）；RC-16/19/20 → V1.x；RC-18 随 M3；RC-21 P2。

---

## 11. 待确认清单（TBD）

| # | 问题 | 默认值 |
|---|---|---|
| TBD-1 | 自动暂停判定：速度阈值与持续时间 | 速度 < 0.5 m/s 持续 10s（真机校准） |
| TBD-2 | 采样策略默认档（1s/5s/10s） | 默认 1s 高质量；设置页可改省电档 |
| TBD-3 | 心率/千卡无外设时的口径 | 心率 `—`；千卡按体重默认 65kg 估算（可改，ACC-02） |
| TBD-4 | 会话中是否允许改类型 | 不允许（P2 评估） |
| TBD-5 | 轻导航提示形式：顶部条 vs 语音 | 顶部条 M1 + 震动；语音 M2 |
| TBD-6 | 放弃是否需要"原因选择" | 不需要 |

---

## 12. 验收清单（QA 用例大纲）

- [ ] 各入口到达准备页 ≤ 2 次点击；定位拒绝后可引导重试（A-RC-01）
- [ ] 记录 5 分钟：指标随时间增长、轨迹生长、暂停计时停（A-RC-02）
- [ ] 锁屏 10 分钟轨迹连续无大跳变；常驻通知可回页（A-RC-03）
- [ ] 记录中强杀 → 重启出现草稿；三个操作可用且数据正确（A-RC-04）
- [ ] 手动计圈 3 次：圈数正确、摘要展示圈数（A-RC-05）
- [ ] 保存后回首页：横幅消失、最近活动新增、概览更新（A-RC-06）
- [ ] 放弃二次确认：误触可复位；确认后无残留（A-RC-07）
- [ ] 会话中从首页/地图再次开始被拦截提示（A-RC-08）
- [ ] 沿路线骑行：预载正确、轻导航条显示、开始后按路线记录（A-RC-09）
- [ ] 摘要改名/备注：默认名可直存，也可展开修改后保存（A-RC-10）

---

*本 PRD 为 v1.1 可修改稿；RC-xx 可追溯主文档 RCD-xx。改完可再做审计。*
